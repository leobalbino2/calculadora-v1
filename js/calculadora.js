function calcular(event) {
    let primeiroValor = parseFloat.getDocumentById(primeiroValor)
    let operador = document.getDocumentByID(operador)
    let segundoValor = parseFloat.getDocumentById(segundoValor)

    event.preventDefault(); // Previne o valor a ser passado na url mas por algum motivo não funciona, talvez pela lôgica ? 
    //fazer switchcase para cada operador
}